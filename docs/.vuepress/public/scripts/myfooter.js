// 尝试去修改页脚(后来发现放到主页的README.md文件中有效)
// window.onload=function(){

    // // ICP备案信息节点
    // var record = document.getElementsByClassName("footer-wrapper footer")[0].children[1]
    // // 创建一个span节点，并填充数据，用于显示公安备案信息
    // var cyberSecurityRecord = document.createElement('span')
    // cyberSecurityRecord.innerHTML=document.getElementsByClassName("footer-wrapper footer")[0].children[4].innerHTML
    // // 设置classname
    // cyberSecurityRecord.className = "cyber-security"
    // // 获取并加上自定义的属性名称
    // cyberSecurityRecord.setAttribute(document.getElementsByClassName("footer-wrapper footer")[0].firstElementChild.getAttributeNames()[0],"")
    
    // // 清空document.getElementsByClassName("footer-wrapper footer")[0]节点，加入自定义的属性
    // document.getElementsByClassName("footer-wrapper footer")[0].innerHTML=""
    // document.getElementsByClassName("footer-wrapper footer")[0].appendChild(record)
    // document.getElementsByClassName("footer-wrapper footer")[0].appendChild(cyberSecurityRecord)
    
// }



